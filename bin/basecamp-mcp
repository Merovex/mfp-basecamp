#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/basecamp_mcp"

case ARGV[0]
when "setup", "config"
  require_relative "../lib/basecamp_mcp/setup"
  ARGV[0] == "setup" ? BasecampMcp::Setup.run : BasecampMcp::Setup.generate_claude_config
else
  BasecampMcp::Server.run
end
